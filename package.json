{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "rm -rf ./node_modules && lerna clean"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.2.4"
  }
}
